# Mirrors CRS 949110: the scoring rules only accumulate, and this refuses the
# request once the total reaches the inbound threshold the spec declares.

SecRule TX:BLOCKING_INBOUND_ANOMALY_SCORE "@ge %{tx.inbound_anomaly_score_threshold}" \
    "id:9009110,\
    phase:2,\
    deny,status:403,\
    t:none,\
    msg:'Inbound Anomaly Score Exceeded (Total Score: %{TX.BLOCKING_INBOUND_ANOMALY_SCORE})',\
    tag:'demo/anomaly'"
